let
    var a:int := 25
    var c := a
    type type1 = int
    type rec = {a : int}
    type rec_arr = array of a_rec
    function main(chiffre : int, c2 : int) : void =
        let
            function foo(chiffre : int) : int =
                1
            var b:string := "bing chilling"
            var d := foo(2)
            var e := 2+3
            var f := a
            var g := d+c+e
            var h := d>c
            var i := d=c
            var j := b=b
            var k := rec{a = 2}
            var l := rec_arr[2] of nil
            var m := rec_arr[2] of rec{a = 2}
            var n:type1 := 2
            function bar(chiffre : int) : int =
                1
        in
            print(foo(2));
            print(bar(3))
        end
    in
        main(69)
    end