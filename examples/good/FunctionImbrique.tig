let
    var a:int := 25

    function main(chiffre : int) : void =
        let
            function foo(chiffre : int) : int =
                1
            var b:String := "bing chilling"
            function bar(chiffre : int) : int =
                1
        in
            print(foo(2));
            print(bar(3))
        end
    in
        main(69)
    end